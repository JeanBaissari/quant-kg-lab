# quant-kg-lab — Roadmap

**Type**: Guide
**Status**: current
**Last Verified**: 2026-08-17

> **Mission**: consolidate the 28 library graphs + skills into a single **gold standard**
> — verifiable, uniform, and composable — then keep the truth surface mechanically honest.
> Headline numbers are generated by `scripts/emit_counts.py` — never hand-edit counts.

## State of the repo

**Gold standard reached for all 28 libraries**: 142 spec-normalized skills (27 routers + 97 modules + 18 playbooks); 1,207 graph-node citations (0 dangling); 71/71 cross-library bridges; 127-doc governed corpus; provenance-gated CI.

| Layer | State |
|-------|-------|
| Knowledge graphs | **28/28 extracted** (92,154 nodes, 229,921 edges) — every graph passes the quality gate |
| Node descriptions | **28/28 ≥ 80%** (c2 green) — `describe_nodes.py --auto` via opencode; curated manifests for Cython/C-only surfaces (ADR-0008) |
| Community labels | **28/28 real** (c1 green) |
| Quality Gate | **28/28 GREEN** (c1–c6) — `scripts/graph_gate.py --ci --all` exits 0 |
| Skills | **142 `SKILL.md`** (27 routers + 97 modules + 18 playbooks); citation gate **1,207/0**; `--require-complete` 28/28 in CI |
| Skill validation | validator v2 (sections/hash/related-skills, module-scoped API) gates `--ci`; 2×14 API live-check matrix (QKG_070) |
| Cross-library bridges | **71/71 resolved**, injected as `_cross_library` overlay (63 nodes) |
| Docs | 127-doc governed corpus, `doc_audit.py --ci` green; counts CI-gated (QKG_065) |
| CI | lint + docs + provenance + artifact-safety + release + pages + nightly-publish active; truth-counts diff gate (QKG_065) |

## Completed paths

| Path | Scope | QKG |
|------|-------|-----|
| 0 | Truth reset & foundations — specs, `graphs.lock`, `rebuild_graph.sh`, `CLAUDE.md` | QKG_001–005 |
| 1 | Rebuild all graphs from scratch (tree-sitter + graphify assistant mode, no LLM key) | QKG_006–010 |
| 2 | Normalize all skills to `SKILL_SPEC` — one template, one frontmatter, retired 3 generations | QKG_011–013 |
| 3 | Verifiable skills — validation hardened, provenance gate, CI installs all 28 libs | QKG_007–008 |
| 4 | Composable quant stack — cross-library bridges, playbooks, unified-index v2 | QKG_014–017 |
| 5 | Docs standardization — naming convention, methodology, before/after narrative | QKG_015–016 |
| 6 | Library expansion — 20 more libraries onboarded via QKG_018 playbook, all pass c1–c6 day one | QKG_018–020 |
| 7 | Trust audit close-out — canonical counts (QKG_065), QUICKSTART (QKG_066), GRAPH_REPORT truth (QKG_068), line-aware citations (QKG_069), 2×14 API matrix (QKG_070), pin→version (QKG_071), skill repairs (QKG_072), curated descriptions (QKG_073), statsmodels depth (QKG_074) | QKG_065–074 |
| 8 | CLI, cross-lib search, quality scores, versioning, freshness | QKG_075–079 |
| 9 | CI consolidation, BM25, quality gates, freshness PRs, playbooks, node_type | QKG_080–085 |

## Backlog

Deferred work — not yet prioritized:

- Rebalance skill depth from clean god-nodes (expand pandas/backtrader/ta-lib; split overloaded files) — follow-up from QKG_020 context
- Expand API live-check matrix beyond the current 2×14 CI job (QKG_070)
- Additional playbooks for domain-specific workflows (e.g. factor research, options pricing)

## Links

- **Specs**: `docs/specs/SKILL_SPEC.md`, `docs/specs/GRAPH_SPEC.md`
- **Quality gate**: `docs/specs/GRAPH_SPEC.md` §5
- **Pinned commits**: `graphs.lock`
- **Onboarding checklist**: `docs/guides/onboarding-checklist.md`
- **Session summary**: `docs/reference/session-summary-2026-08-16.md`
- **Counts**: `scripts/emit_counts.py --markdown`

*Last verified against the repo: 2026-08-17.*
